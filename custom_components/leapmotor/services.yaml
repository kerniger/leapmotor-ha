lock:
  name: Lock vehicle
  description: Lock a Leapmotor vehicle. Requires the configured Vehicle PIN.
  fields:
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      example: LPXXXXXXXXXXXXXXX
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      example: lock.leapmotor_vehicle_lock
      selector:
        text:
unlock:
  name: Unlock vehicle
  description: Unlock a Leapmotor vehicle. Requires the configured Vehicle PIN.
  fields:
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
unlock_charger:
  name: Unlock charger
  description: Unlock the charging gun/charger connector for unplugging. Requires the configured Vehicle PIN.
  fields:
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
trunk_open:
  name: Open trunk
  description: Open the trunk. Requires the configured Vehicle PIN.
  fields:
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
trunk_close:
  name: Close trunk
  description: Close the trunk. Requires the configured Vehicle PIN.
  fields:
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
find_car:
  name: Find vehicle
  description: Trigger the horn/find-vehicle action. Requires the configured Vehicle PIN.
  fields:
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
sunshade_open:
  name: Open sunshade
  description: Open the sunshade. Requires the configured Vehicle PIN. Optionally set a partial position from 0 to 10; omit for fully open.
  fields:
    value:
      name: Position
      description: Optional sunshade position, 0 to 10. Omit for fully open.
      example: 5
      selector:
        number:
          min: 0
          max: 10
          step: 1
          mode: box
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
sunshade_close:
  name: Close sunshade
  description: Close the sunshade. Requires the configured Vehicle PIN. Optionally set a partial position from 0 to 10; omit for fully closed.
  fields:
    value:
      name: Position
      description: Optional sunshade position, 0 to 10. Omit for fully closed.
      example: 0
      selector:
        number:
          min: 0
          max: 10
          step: 1
          mode: box
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
windows_open:
  name: Open windows
  description: Open the windows. Requires the configured Vehicle PIN. Optionally set a partial position from 0 to 100; omit for fully open.
  fields:
    value:
      name: Position
      description: Optional window position, 0 to 100. Omit for fully open.
      example: 30
      selector:
        number:
          min: 0
          max: 100
          step: 1
          mode: slider
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
windows_close:
  name: Close windows
  description: Close the windows. Requires the configured Vehicle PIN. Optionally set a partial position from 0 to 100; omit for fully closed.
  fields:
    value:
      name: Position
      description: Optional window position, 0 to 100. Omit for fully closed.
      example: 0
      selector:
        number:
          min: 0
          max: 100
          step: 1
          mode: slider
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
ac_off:
  name: Climate off
  description: Turn climate control fully off. Requires the configured Vehicle PIN.
  fields:
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
set_climate:
  name: Set climate
  description: Send a parameterised climate command to the vehicle. Requires the configured Vehicle PIN.
  fields:
    mode:
      name: Mode
      description: Climate mode.
      required: true
      example: cold
      selector:
        select:
          options:
            - cold
            - hot
            - wind
            - nohotcold
    temperature:
      name: Temperature
      description: Target temperature in °C, from 18 to 32. Defaults to 26.
      example: 22
      selector:
        number:
          min: 18
          max: 32
          step: 1
          mode: box
    fan_speed:
      name: Fan speed
      description: Fan level from 1 to 7. Defaults to 3.
      example: 4
      selector:
        number:
          min: 1
          max: 7
          step: 1
          mode: slider
    recirculate:
      name: Recirculate
      description: Use cabin air recirculation instead of outside air. Defaults to false.
      example: false
      selector:
        boolean:
    windshield_defrost:
      name: Windshield defrost
      description: Enable front windshield defrost. Defaults to false.
      example: false
      selector:
        boolean:
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
set_climate_schedule:
  name: Set climate schedule
  description: Replace all climate pre-conditioning schedules with one schedule entry. Requires the configured Vehicle PIN. The start time must be in the future in vehicle/app local time.
  fields:
    start_time:
      name: Start time
      description: Future schedule start time, for example 2026-06-09 07:30:00.
      required: true
      example: "2026-06-09 07:30:00"
      selector:
        datetime:
    mode:
      name: Mode
      description: Schedule mode. Use nohotcold for ventilation or no heat/cool selection.
      example: cold
      selector:
        select:
          options:
            - cold
            - hot
            - nohotcold
    operate:
      name: Operation
      description: App operation value. Most schedules use manual; app defrost/no-selection presets use auto.
      example: manual
      selector:
        select:
          options:
            - manual
            - auto
    temperature:
      name: Temperature
      description: Target temperature in °C, from 18 to 32. Defaults to 26.
      example: 22
      selector:
        number:
          min: 18
          max: 32
          step: 1
          mode: box
    fan_speed:
      name: Fan speed
      description: Fan level from 1 to 7. Defaults to 4.
      example: 4
      selector:
        number:
          min: 1
          max: 7
          step: 1
          mode: slider
    recirculate:
      name: Recirculate
      description: Use cabin air recirculation instead of outside air. Defaults to false.
      example: false
      selector:
        boolean:
    windshield_defrost:
      name: Windshield defrost
      description: Enable front windshield defrost for the schedule. Defaults to false.
      example: false
      selector:
        boolean:
    days:
      name: Days
      description: Optional repeat days where 0=Sunday through 6=Saturday. Leave empty for a one-time schedule.
      example: [1, 2, 3, 4, 5]
      selector:
        select:
          multiple: true
          options:
            - label: Sunday
              value: "0"
            - label: Monday
              value: "1"
            - label: Tuesday
              value: "2"
            - label: Wednesday
              value: "3"
            - label: Thursday
              value: "4"
            - label: Friday
              value: "5"
            - label: Saturday
              value: "6"
    enabled:
      name: Enabled
      description: Whether the schedule entry is active. Defaults to true.
      example: true
      selector:
        boolean:
    set_id:
      name: Set ID
      description: Optional existing opaque app schedule ID to reuse when editing. Leave empty for a new schedule.
      selector:
        text:
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
cancel_climate_schedule:
  name: Cancel climate schedule
  description: Cancel all climate pre-conditioning schedules by sending an empty controls list. Requires the configured Vehicle PIN.
  fields:
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
prepare_car:
  name: Prepare car
  description: Run one-touch vehicle preparation immediately with climate, front-seat comfort, steering-wheel heat, mirror heat, and optional navigation destination. Requires the configured Vehicle PIN.
  fields:
    climate_enabled:
      name: Climate enabled
      description: Include the air-conditioning preparation block. Defaults to true.
      example: true
      selector:
        boolean:
    mode:
      name: Climate mode
      description: Preparation climate mode.
      example: cold
      selector:
        select:
          options:
            - cold
            - hot
            - nohotcold
    operate:
      name: Operation
      description: App operation value. Most preparations use manual.
      example: manual
      selector:
        select:
          options:
            - manual
            - auto
    temperature:
      name: Temperature
      description: Target temperature in °C, from 18 to 32. Defaults to 18.
      example: 18
      selector:
        number:
          min: 18
          max: 32
          step: 1
          mode: box
    fan_speed:
      name: Fan speed
      description: Fan level from 1 to 7. Defaults to 7.
      example: 7
      selector:
        number:
          min: 1
          max: 7
          step: 1
          mode: slider
    recirculate:
      name: Recirculate
      description: Use cabin air recirculation instead of outside air. Defaults to true.
      example: true
      selector:
        boolean:
    windshield_defrost:
      name: Windshield defrost
      description: Enable front windshield defrost. Defaults to false.
      example: false
      selector:
        boolean:
    driver_seat:
      name: Driver seat
      description: Driver seat preparation mode. Active modes use the selected level.
      example: heat
      selector:
        select:
          options:
            - "off"
            - heat
            - ventilation
    driver_seat_level:
      name: Driver seat level
      description: Driver seat level from 1 to 3. Defaults to 3.
      example: 3
      selector:
        number:
          min: 1
          max: 3
          step: 1
          mode: slider
    passenger_seat:
      name: Passenger seat
      description: Passenger seat preparation mode. Active modes use the selected level.
      example: ventilation
      selector:
        select:
          options:
            - "off"
            - heat
            - ventilation
    passenger_seat_level:
      name: Passenger seat level
      description: Passenger seat level from 1 to 3. Defaults to 3.
      example: 3
      selector:
        number:
          min: 1
          max: 3
          step: 1
          mode: slider
    steering_wheel_heat:
      name: Steering wheel heat
      description: Include steering-wheel heating. Defaults to false.
      example: false
      selector:
        boolean:
    mirror_heat:
      name: Mirror heat
      description: Include exterior mirror heating. Defaults to false.
      example: false
      selector:
        boolean:
    destination_name:
      name: Destination name
      description: Optional navigation destination name. Latitude and longitude are required when a destination is used.
      example: Bern Bahnhof
      selector:
        text:
    destination_address:
      name: Destination address
      description: Optional navigation destination address. Defaults to the destination name.
      example: Bahnhofplatz, Bern, Schweiz
      selector:
        text:
    destination_latitude:
      name: Destination latitude
      description: Optional navigation destination latitude.
      example: 46.94809
      selector:
        number:
          min: -90
          max: 90
          mode: box
    destination_longitude:
      name: Destination longitude
      description: Optional navigation destination longitude.
      example: 7.43914
      selector:
        number:
          min: -180
          max: 180
          mode: box
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
set_prepare_car_schedule:
  name: Set prepare-car schedule
  description: Replace all one-touch vehicle preparation schedules with one schedule entry. Requires the configured Vehicle PIN.
  fields:
    start_time:
      name: Start time
      description: Schedule start time, for example 2026-06-09 07:30:00.
      required: true
      example: "2026-06-09 07:30:00"
      selector:
        datetime:
    days:
      name: Days
      description: Optional repeat days where 0=Sunday through 6=Saturday. Leave empty for a one-time schedule.
      example: [1, 2, 3, 4, 5]
      selector:
        select:
          multiple: true
          options:
            - label: Sunday
              value: "0"
            - label: Monday
              value: "1"
            - label: Tuesday
              value: "2"
            - label: Wednesday
              value: "3"
            - label: Thursday
              value: "4"
            - label: Friday
              value: "5"
            - label: Saturday
              value: "6"
    enabled:
      name: Enabled
      description: Whether the schedule entry is active. Defaults to true.
      example: true
      selector:
        boolean:
    set_id:
      name: Set ID
      description: Optional existing opaque app schedule ID to reuse when editing. Leave empty for a new schedule.
      selector:
        text:
    climate_enabled:
      name: Climate enabled
      description: Include the air-conditioning preparation block. Defaults to true.
      example: true
      selector:
        boolean:
    mode:
      name: Climate mode
      description: Preparation climate mode.
      example: cold
      selector:
        select:
          options:
            - cold
            - hot
            - nohotcold
    operate:
      name: Operation
      description: App operation value. Most preparations use manual.
      example: manual
      selector:
        select:
          options:
            - manual
            - auto
    temperature:
      name: Temperature
      description: Target temperature in °C, from 18 to 32. Defaults to 18.
      example: 18
      selector:
        number:
          min: 18
          max: 32
          step: 1
          mode: box
    fan_speed:
      name: Fan speed
      description: Fan level from 1 to 7. Defaults to 7.
      example: 7
      selector:
        number:
          min: 1
          max: 7
          step: 1
          mode: slider
    recirculate:
      name: Recirculate
      description: Use cabin air recirculation instead of outside air. Defaults to true.
      example: true
      selector:
        boolean:
    windshield_defrost:
      name: Windshield defrost
      description: Enable front windshield defrost. Defaults to false.
      example: false
      selector:
        boolean:
    driver_seat:
      name: Driver seat
      description: Driver seat preparation mode. Active modes use the selected level.
      example: heat
      selector:
        select:
          options:
            - "off"
            - heat
            - ventilation
    driver_seat_level:
      name: Driver seat level
      description: Driver seat level from 1 to 3. Defaults to 3.
      example: 3
      selector:
        number:
          min: 1
          max: 3
          step: 1
          mode: slider
    passenger_seat:
      name: Passenger seat
      description: Passenger seat preparation mode. Active modes use the selected level.
      example: ventilation
      selector:
        select:
          options:
            - "off"
            - heat
            - ventilation
    passenger_seat_level:
      name: Passenger seat level
      description: Passenger seat level from 1 to 3. Defaults to 3.
      example: 3
      selector:
        number:
          min: 1
          max: 3
          step: 1
          mode: slider
    steering_wheel_heat:
      name: Steering wheel heat
      description: Include steering-wheel heating. Defaults to false.
      example: false
      selector:
        boolean:
    mirror_heat:
      name: Mirror heat
      description: Include exterior mirror heating. Defaults to false.
      example: false
      selector:
        boolean:
    destination_name:
      name: Destination name
      description: Optional navigation destination name. Latitude and longitude are required when a destination is used.
      example: Bern Bahnhof
      selector:
        text:
    destination_address:
      name: Destination address
      description: Optional navigation destination address. Defaults to the destination name.
      example: Bahnhofplatz, Bern, Schweiz
      selector:
        text:
    destination_latitude:
      name: Destination latitude
      description: Optional navigation destination latitude.
      example: 46.94809
      selector:
        number:
          min: -90
          max: 90
          mode: box
    destination_longitude:
      name: Destination longitude
      description: Optional navigation destination longitude.
      example: 7.43914
      selector:
        number:
          min: -180
          max: 180
          mode: box
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
cancel_prepare_car_schedule:
  name: Cancel prepare-car schedule
  description: Cancel all one-touch vehicle preparation schedules by sending an empty controls list. Requires the configured Vehicle PIN.
  fields:
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
quick_cool:
  name: Quick cool
  description: Trigger the verified quick-cool profile. Requires the configured Vehicle PIN.
  fields:
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
quick_heat:
  name: Quick heat
  description: Trigger the verified quick-heat profile. Requires the configured Vehicle PIN.
  fields:
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
windshield_defrost:
  name: Windshield defrost
  description: Trigger the verified windshield-defrost profile. Requires the configured Vehicle PIN.
  fields:
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
set_charge_limit:
  name: Set charge limit
  description: Set the charging upper limit while preserving the current charging schedule from the vehicle. Requires the configured Vehicle PIN.
  fields:
    charge_limit_percent:
      name: Charge limit
      description: New upper charge limit in percent.
      required: true
      example: 85
      selector:
        number:
          min: 1
          max: 100
          mode: box
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        text:
send_destination:
  name: Send destination
  description: Send a navigation destination to the vehicle. Does not require the Vehicle PIN.
  fields:
    name:
      name: Destination name
      description: Short destination label shown in the vehicle navigation.
      required: true
      example: Bern
      selector:
        text:
    address:
      name: Address
      description: Full destination address. Defaults to the destination name when omitted.
      example: Bern, Schweiz
      selector:
        text:
    latitude:
      name: Latitude
      description: Destination latitude.
      required: true
      example: 46.9479739
      selector:
        number:
          min: -90
          max: 90
          mode: box
    longitude:
      name: Longitude
      description: Destination longitude.
      required: true
      example: 7.4474468
      selector:
        number:
          min: -180
          max: 180
          mode: box
    vin:
      name: VIN
      description: Target VIN. Required when multiple vehicles are configured.
      selector:
        text:
    entity_id:
      name: Entity ID
      description: Optional Leapmotor entity to resolve the target vehicle.
      selector:
        entity:
          integration: leapmotor
export_diagnostics:
  name: Export diagnostics
  description: Write a redacted Leapmotor diagnostics JSON file to /config/leapmotor.
  fields:
    filename:
      name: Filename
      description: Optional JSON filename. Directory components are ignored.
      example: leapmotor-diagnostics.json
      selector:
        text:
